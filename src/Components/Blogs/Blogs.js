import React from 'react';

const Blogs = () => {
    return (
        <div>
            <h1>Context api purpose ?</h1>
            <p>big application has many components. then data passing is most complicated. this time application needs a center state/store to pass easily data pass. context API solves that problem. it is easy to use.</p>
            <hr />
            <h1>Semantic Tags ?</h1>
            <p>semantics is the most important feature of HTML5 is. Semantic tag refers to syntax that makes the HTML easier to understand the different sections and layout of web pages. It makes web pages more informative and strong. For example article,aside,details,figure,footer,header.</p>
        </div>
    );
};

export default Blogs;